# Go compiled binaries
echoes-engine
*.exe

# Go module artifacts
vendor/

# Local database persistence (if mounted locally instead of via named volume)
.pgdata/
*.db

# Environment variables
.env
roe-server/node_modules/
roe-server/db/*.db
roe-server/db/*.db-wal

# ignore the docker compose files
docker-compose.yml
docker-compose.override.ymls
